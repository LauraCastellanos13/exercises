// ES6-Set es una colección de elementos sin un orden en particular, en donde cada elemento puede aparecer una sola vez.
const list = new Set();

list.add("item 1");
list.add("item 2").add("item 3");

console.log(list);